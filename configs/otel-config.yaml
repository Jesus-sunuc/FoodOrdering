receivers:
  filelog:
    include:
      - /var/log/app/*.log
    start_at: beginning

exporters:
  loki:
    endpoint: http://lunchbox6.duckdns.org:3100/loki/api/v1/push
  debug:
    verbosity: detailed

service:
  pipelines:
    logs:
      receivers: [filelog]
      exporters: [loki, debug]
